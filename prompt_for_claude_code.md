# Prompt สำหรับ Claude Code — วาด kinematic link ของหุ่นสี่ขา 12 DOF

> คัดลอกทุกอย่างตั้งแต่เส้นแบ่งด้านล่างไปวางใน Claude Code

---

สร้างสคริปต์ Python ไฟล์เดียวสำหรับวาดและตรวจสอบโครงสร้าง kinematic ของหุ่นสี่ขา 12 DOF (4 ขา × 3 ข้อต่อ) โดยใช้ standard DH convention

## ข้อตกลงเฟรมอ้างอิง

- เฟรมลำตัว `{B}` อยู่ที่ศูนย์กลางลำตัว: `x` ชี้ไปข้างหน้า, `y` ชี้ไปทางซ้าย, `z` ชี้ขึ้นบน
- ใช้ **standard DH** (แบบ Spong): `z_{i-1}` คือแกนหมุนของข้อต่อที่ `i`, `x_i` คือเส้นตั้งฉากร่วมระหว่าง `z_{i-1}` กับ `z_i`
- แต่ละขามี 3 ข้อต่อ: `θ1` = abduction (หมุนรอบแกน x ของลำตัว), `θ2` = hip pitch, `θ3` = knee
- ท่า zero pose คือ `θ1 = θ2 = θ3 = 0` ขาเหยียดตรงดิ่งลงพื้น และที่ท่านี้ `x0 = x1 = x2 = x3` ชี้ลงทั้งหมด

## โครงสร้างของขาหนึ่งข้าง

จากลำตัวไล่ลงไปตามลำดับ:

1. จากศูนย์กลางลำตัว เยื้องไป `(±a, ±b, -c)` ถึงแกน `z0` (แกน abduction ทิศทางตามแกน x ของลำตัว)
2. จากแกน `z0` ดิ่งลง `h` ถึงแกน `z1` (แกน hip pitch ทิศทางตามแกน y) — แกนสองเส้นนี้ไขว้กันแบบตั้งฉาก
3. จากแกน `z1` เยื้องออกด้านข้าง `D1` ถึงตำแหน่งเซอร์โว hip pitch จริง
4. ต้นขายาว `L1` ลงไปถึงแกน `z2` (แกน knee ขนานกับ `z1`)
5. ที่เข่าเยื้องกลับเข้าหาลำตัว `D2` แล้วแข้งยาว `L2` ลงไปถึงปลายเท้า

## ตาราง DH ที่ต้องใช้

| i | θᵢ | dᵢ | aᵢ | αᵢ |
|---|----|----|----|----|
| 1 | θ1 | 0   | h  | -90° |
| 2 | θ2 | D1  | L1 | 0 |
| 3 | θ3 | -D2 | L2 | 0 |

transform คงที่จากลำตัวไปเฟรม 0 (ไม่ใช่แถว DH):

```
T_B0 = Trans(±a, ±b, -c) @ Roty(90°)
```

เครื่องหมายของแต่ละขา:

| ขา | a | b | D1 | D2 |
|----|---|---|----|----|
| FL (หน้า–ซ้าย) | + | + | + | + |
| FR (หน้า–ขวา)  | + | − | − | − |
| RL (หลัง–ซ้าย) | − | + | + | + |
| RR (หลัง–ขวา)  | − | − | − | − |

## พารามิเตอร์เริ่มต้น

ใส่เป็น dict หรือ dataclass ไว้บนสุดของไฟล์ แก้ง่าย พร้อมคอมเมนต์บอกความหมายและหน่วย (mm) ทุกตัว ใช้ค่าตั้งต้นนี้ไปก่อน:

```python
a  = 87.5   # ครึ่งระยะสะโพกหน้า-หลัง
b  = 47.5   # ครึ่งระยะสะโพกซ้าย-ขวา
c  = 20.0   # ระยะดิ่งจากศูนย์กลางลำตัวลงมาถึงแกน abduction
h  = 30.0   # ระยะดิ่งจากแกน abduction ลงมาถึงแกน hip pitch
D1 = 55.0   # offset ด้านข้าง จากแกน hip pitch ไปตำแหน่งเซอร์โวจริง
D2 = 55.0   # offset ที่เข่า เยื้องกลับเข้าหาลำตัว
L1 = 120.0  # ความยาวต้นขา
L2 = 130.0  # ความยาวแข้ง
```

## สิ่งที่สคริปต์ต้องทำ

1. **ฟังก์ชัน `dh_transform(theta, d, a, alpha)`** คืนค่า homogeneous transform 4×4 ตาม standard DH
2. **ฟังก์ชัน `leg_fk(leg_name, q)`** รับชื่อขากับมุมข้อต่อ 3 ตัว (เรเดียน) คืนค่า list ของ transform สะสม `[T_B0, T_B1, T_B2, T_B3]` เทียบเฟรมลำตัว เพื่อให้ดึงทั้งตำแหน่งข้อต่อและทิศทางแกนออกมาวาดได้
3. **ฟังก์ชัน `robot_fk(q_all)`** รับมุมทั้ง 12 ตัว คืนค่าตำแหน่งปลายเท้าทั้ง 4 ขาเทียบเฟรมลำตัว
4. **วาดด้วย matplotlib แบบ 3D** ประกอบด้วย:
   - กล่องลำตัวเป็นสี่เหลี่ยมเชื่อมจุดสะโพกทั้ง 4
   - เส้น link ของแต่ละขา วาดผ่านทุกจุด origin ของเฟรม รวมจุดหักมุมที่ offset ด้วย ไม่ใช่ลากตรงจากสะโพกไปเท้า
   - จุด marker ที่ข้อต่อทุกจุด และ marker คนละแบบที่ปลายเท้า
   - แกน `x` `y` `z` ของทุกเฟรมเป็นลูกศรสามสี (แดง เขียว น้ำเงิน) พร้อม label `x0 y0 z0`, `x1 y1 z1`, ... เปิด/ปิดได้ด้วยตัวแปร `SHOW_FRAMES`
   - ตั้ง aspect ratio ให้เท่ากันทุกแกน ไม่งั้นรูปจะบิด
5. **สไลเดอร์** ควบคุม `θ1 θ2 θ3` ของขาที่เลือก (มี dropdown หรือ radio button เลือกขา) และอัปเดตรูปแบบ real-time
6. **โหมดตรวจสอบ** ฟังก์ชัน `verify()` ที่ print ตำแหน่งปลายเท้าทั้ง 4 ขาที่ท่า zero pose ออกมาเทียบกับค่าที่คำนวณมือ

## เกณฑ์ตรวจสอบว่าถูก

ที่ท่า zero pose ปลายเท้าขา FL ต้องอยู่ที่ประมาณ:

```
x = +a
y = +b + D1 - D2
z = -(c + h + L1 + L2)
```

ให้ `verify()` assert ว่าผลจาก FK ตรงกับสูตรนี้ภายใน tolerance 1e-6 ทั้ง 4 ขา ถ้าไม่ตรงแปลว่าตาราง DH หรือเครื่องหมายของขาผิด ให้แก้จนผ่าน

การตรวจเพิ่มเติมที่ควรใส่:

- หมุน `θ1` อย่างเดียว ปลายเท้าต้องเคลื่อนที่เป็นวงกลมในระนาบ y–z และระยะจากแกน `z0` ต้องคงที่
- หมุน `θ2` หรือ `θ3` อย่างเดียว พิกัด `y` ของปลายเท้าต้องไม่เปลี่ยน เพราะ offset ทั้งสองตัววางตัวตามแกนหมุนพอดี
- ถ้า `D1 == D2` ปลายเท้าต้องอยู่ในระนาบเดียวกับแกน `z0` พอดี คือ `y_foot == ±b`

## ข้อกำหนดทางเทคนิค

- ใช้แค่ `numpy` กับ `matplotlib` ไม่ต้องพึ่ง library หุ่นยนต์อื่น
- เขียนไฟล์เดียว รันได้เลยด้วย `python leg_kinematics.py`
- ใส่ type hint และ docstring สั้น ๆ ทุกฟังก์ชัน
- คอมเมนต์เป็นภาษาไทยได้
- อย่า hardcode ค่าตัวเลขซ้ำในหลายที่ ให้อ้างจาก dict พารามิเตอร์ตัวเดียว
- รันสคริปต์และรัน `verify()` ให้ผ่านก่อนบอกว่าเสร็จ

## หมายเหตุ

`h` อาจเป็น 0 ถ้าแกน abduction กับแกน hip pitch ตัดกันพอดี สคริปต์ต้องทำงานถูกต้องทั้งกรณี `h = 0` และ `h ≠ 0` เช่นเดียวกับ `c`
